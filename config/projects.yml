# Every public GitHub repository to track.
# Orpheon itself is excluded so the journal does not document its own deploys.
# Empty or placeholder repos still appear; the journal will say so honestly.
projects:
  - repository: venvennnn/aftermath
    slug: aftermath
    enabled: true

  - repository: venvennnn/causalens
    slug: causalens
    enabled: true

  - repository: venvennnn/janus
    slug: janus
    enabled: true

  - repository: venvennnn/orbitcast
    slug: orbitcast
    enabled: true

  - repository: venvennnn/orbitcast_android
    slug: orbitcast-android
    enabled: true

  - repository: venvennnn/findhackathons
    slug: findhackathons
    enabled: true

  - repository: venvennnn/forest_sentinel
    slug: forest-sentinel
    enabled: true

  - repository: venvennnn/underwritten_autonomy
    slug: underwritten-autonomy
    enabled: true

  - repository: venvennnn/orpheus
    slug: orpheus
    enabled: true

  - repository: venvennnn/physai_track2
    slug: physai-track2
    enabled: true

  - repository: venvennnn/theemailgame
    slug: the-email-game
    enabled: true

  - repository: venvennnn/google_waxal_challenge
    slug: google-waxal
    enabled: true

  - repository: venvennnn/sitrep_venvenn
    slug: sitrep
    enabled: true

  - repository: venvennnn/humor_genome_kaggle
    slug: humor-genome
    enabled: true
